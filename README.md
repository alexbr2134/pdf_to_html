# pdf-to-html

PDF → HTML для **векторных** PDF.

Основной пайплайн — **smart** (`pdf_to_html_smart.ipynb`): детект таблиц + семантическая сборка grid (colspan/rowspan, подписи, prose) + полный документ (текст и таблицы в порядке чтения).

Бейзлайны для сравнения:

- `pdf_to_html_pdfplumber.ipynb` — pdfplumber tables + words
- `pdf_to_html_camelot.ipynb` — Camelot extract + текст вне таблиц

## Алгоритм (smart)

```
страница PDF
  │
  ├─ (опц.) векторизация линий скана → pdf_line_vectorize
  │
  ├─ find_tables_smart
  │     Camelot detect
  │       ├─ нет таблиц → pdfplumber lines → иначе text → иначе Camelot extract
  │       ├─ есть + lines → pdfplumber lines
  │       └─ есть + lines пусто → pdfplumber text / Camelot
  │
  ├─ для каждой таблицы: process_table
  │     build_cells (слова pdfplumber → ячейки)
  │     merge phantom / leading text columns
  │     classify HEADER/DATA
  │     merge wrapped / label rows (кроме стека кодов)
  │     restore colspan/rowspan по bbox и правилам подписей
  │     enrich side labels (ОКУД/ОКПО слева от «Коды»)
  │     code-stack colspan: одна ячейка кода накрывает value-колонки;
  │       три колонки только у строки даты (число | месяц | год)
  │
  ├─ сборка страницы
  │     таблицы + free text в reading order
  │     prose-fallback, если сетка выродилась в абзацы
  │
  └─ документ
        page suitability / роутинг (`page_suitability.py`):
          pre: битые шрифты (`broken_fonts`) / image-only скан
          post (после process_table, векторизация сохраняется):
            линии брались с растра (`vectorized`) и
            (много colspan/rowspan ИЛИ крупный размер таблицы)
            → `unmarked_table_lines`, страница для другой модели
        wrap HTML (thead/tbody, spans, шрифты из PDF)
```

Текст ячеек всегда из pdfplumber (`extract_words`); Camelot даёт только структуру/детект. Smart дополнительно восстанавливает семантику: объединённые ячейки, переносы подписей, вложенные таблицы кодов, разделение прозы и табличных блоков.

**Роутинг / отсев страниц** (`skip_unsuitable=True` по умолчанию):

| Причина | Когда | Что в HTML |
|---|---|---|
| `broken_fonts` | OCR-garble / битая кодировка (pre) | заглушка, без smart-сборки |
| `image_only_scan` | почти нет текста при большом растре (pre) | заглушка |
| `unmarked_table_lines` | растровые линии + после обработки много span **или** крупный grid (post) | заглушка; векторизация при проверке уже была |

Мелкие/базовые таблицы и PDF с уже размеченными lines/edges/rects (например `2508064833-*`) не роутятся. В конце экспорта — сводка `принято` / `отсеяно`. Отключить: `skip_unsuitable=False`.

## Сравнение с бейзлайнами

### Методика оценки

- Шкала **1–10**: пригодность HTML для LLM (таблицы, заголовки, иерархия, смысл ячеек, полнота).
- **Оценщик:** Cursor Grok 4.5 (ручной отсмотр HTML, не авто-метрика).
- HTML: `парсинг html данные/` (smart), `парсинг html pdfplumber/`, `парсинг html camelot/`.
- Выборка: **20** одностраничных PDF из `парсинг pdf данные/`.
- **Smart:** оценка только если страница **не отсеяна роутингом** (`—` = роут, в среднее не входит). Актуальный прогон: **принято 12 / 20**.
- **pdfplumber / camelot:** замороженные ручные оценки того же набора (бейзлайны роутинг не используют).
- Среднее smart: **n=12** принятых; без пустой формы `chet_f` — **n=11**.
- **Время:** ранее замерено на n=16 (CPU Intel i3), с/стр полного пайплайна.

### Оценки по файлам

| Файл | smart | pdfplumber | camelot | Роут / заметка |
|---|:-:|:-:|:-:|---|
| `1655388160-6` | — | 2 | 1 | роут `broken_fonts` (битый OCR) |
| `1655388160-14` | — | 3 | 3 | роут `broken_fonts` |
| `1655388160-26` | — | 2 | 1 | роут `broken_fonts`; camelot — ложные table |
| `2508064833-12` | **9** | 5 | 5 | линии в PDF размечены → не роутится; ОС rowspan/colspan |
| `2508064833-15` | **9** | 6 | 6 | не роутится; многоуровневые spans финвложений |
| `2508064833-17` | **9** | 8 | 8 | не роутится; оценочные обязательства, `<th>` |
| `2508064833-31` | **8** | **8** | 3 | не роутится; prose учетной политики |
| `2703000015-6` | **8** | 3 | 6 | принято; баланс + коды; лёгкий OCR-шум |
| `2703000015-9` | — | 3 | **7** | роут `unmarked_table_lines` (крупная сетка на растре) |
| `2703000015-20` | **8** | 4 | 5 | принято; таблица оплат (частично рвёт строки) |
| `2703000015-24` | — | 3 | **7** | роут `unmarked_table_lines` (span + размер) |
| `2703000015-39` | **8** | **8** | 2 | принято; связный текст; camelot false table |
| `2703000015-46` | — | 4 | 7 | роут `unmarked_table_lines` (крупная таблица) |
| `2703000015-96` | — | 1 | 4 | роут `unmarked_table_lines` |
| `2703000015-111` | — | 2 | 5 | роут `unmarked_table_lines` (много colspan) |
| `chet_f` | **3** | **6** | **6** | принято; пустая форма → prose без grid |
| `chet_f_2` | **8** | 7 | 4 | принято; счет-фактура со spans |
| `ks3` | **8** | 5 | 5 | принято; основная таблица работ ок |
| `obrazec-schyota` | **8** | 6 | 3 | принято; spans bank block |
| `sfv_2012` | **8** | 7 | 7 | принято; invoice headers со spans |

### Сводка (качество + время)

| Метод | Качество | Лучший / ничья (среди оценённых smart) | Время, с/стр |
|---|:-:|:-:|:-:|
| **smart** | **7.8** (n=12 принято); **8.3** без `chet_f` (n=11) | **11 / 12** | **1.76** |
| camelot | 5.3 (n=16 ист.) | — | 1.13 |
| pdfplumber | 5.0 (n=16 ист.) | — | 0.26 |

На выборке из 20: роутинг снял **8** стр. (3× `broken_fonts`, 5× `unmarked_table_lines`). Качество smart: сумма баллов принятых / 12 = **94/12 = 7.8**; без `chet_f` **91/11 = 8.3**.


